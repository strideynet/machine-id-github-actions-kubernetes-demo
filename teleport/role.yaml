kind: role
version: v7
metadata:
  name: github-actions-blog-demo
spec:
  allow:
    kubernetes_labels:
      '*': '*'
    kubernetes_groups:
      - github-ci
    kubernetes_resources:
      - kind: "*"
        namespace: "*"
        name: "*"